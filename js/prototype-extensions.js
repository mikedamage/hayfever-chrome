// Generated by CoffeeScript 1.4.0

/*
JavaScript Core Prototype Extensions
*/


/*
Convert string to slug
*/


(function() {

  String.prototype.toSlug = function() {
    return this.replace(/[^a-zA-Z0-9\s]/g, '').toLowerCase().replace(/\s/g, '_');
  };

  /*
  Convert a decimal time to clock time (sexagesimal)
  */


  Number.prototype.toClockTime = function() {
    var decimal_split, hours, minutes, string_val, time;
    if (this === 0) {
      return '0:00';
    }
    string_val = this.toFixed(2);
    decimal_split = string_val.split('.');
    hours = parseInt(decimal_split[0], 10);
    minutes = parseFloat("0." + decimal_split[1]);
    minutes = parseInt(minutes * 60, 10);
    if (minutes < 10) {
      time = "" + hours + ":0" + minutes;
    } else {
      time = "" + hours + ":" + minutes;
    }
    return time;
  };

  /*
  Get Yesterday's Date
  */


  Date.prototype.yesterday = function() {
    return new Date(this.getTime() - 86400000);
  };

  /*
  Format the date so Harvest can understand it
  */


  Date.prototype.toHarvestString = function() {
    var arr;
    arr = this.toDateString().split(' ');
    return "" + arr[0] + ", " + arr[2] + " " + arr[1] + " " + arr[3];
  };

  /*
  Get day of year
  */


  Date.prototype.getDOY = function() {
    var jan_one;
    jan_one = new Date(this.getFullYear(), 0, 1);
    return Math.ceil((this - jan_one) / 86400000);
  };

  /*
  Array any() function
  */


  Array.prototype.any = function() {
    return this.length > 0;
  };

}).call(this);
